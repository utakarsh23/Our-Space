import React from "react";
import ReactDOM from "react-dom/client";
import RomanticNotesApp from "./RomanticNotesApp";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
    <React.StrictMode>
        <RomanticNotesApp />
    </React.StrictMode>
);